{% extends '::layout/baseAdmin.html.twig' %}

{% block body %}
    <h1>Pages list</h1>


    <table class="table table-striped table-hover">
        <thead>
            <tr>
{#                <th>Id</th>#}
                <th>Titre</th>
                    {#                <th>Contenu</th>#}
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for page in pages %}
                <tr>
{#                    <td>{{ page.id }}</td>#}
                    <td><a href="{{ path('admin_pages_show', { 'id': page.id }) }}">{{ page.titre }}</a></td>
                    {#                <td>{{ page.contenu }}</td>#}
                    <td>
                        <a href="{{ path('admin_pages_show', { 'id': page.id }) }}"><i class="icon-book"></i></a>
                        <a href="{{ path('admin_pages_edit', { 'id': page.id }) }}"><i class="icon-edit"></i></a>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

            <a class="btn btn-primary" href="{{ path('admin_pages_new') }}">Nouvelle page</a>
{% endblock %}
